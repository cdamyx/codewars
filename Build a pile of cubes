function findNb(m) {
  let sum = 0;
  let count = 0;
  for(let i = 1; sum < m; i++){
    sum += Math.pow(i, 3);
    count = i;
  }
  return sum === m ? count : -1;
}

//time: O(n), space: O(1)? need to review space complexity
